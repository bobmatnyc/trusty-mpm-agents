# trusty-mpm-agents

Agent and skill distribution for trusty-mpm sessions.

## Structure

- `agents/*.md` — Agent definitions deployed to `~/.claude/agents/` on `tm launch`
- `skills/*.md` — Skill definitions deployed to `~/.claude/skills/` on `tm launch`
